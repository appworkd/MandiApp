<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.appwork.mandiapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".UserListActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CustomerBillDetailsActivity" />
        <activity
            android:name=".AadatChargeActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".ConsentActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".ChargesActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".RatesActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".WeightActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".SplashActivity"
            android:theme="@style/AppTheme.SplashTheme">

        </activity>
    </application>

</manifest>